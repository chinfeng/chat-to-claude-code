{
  "name": "chat-to-claude-code",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run src/server/index.ts",
    "build": "bun build src/server/index.ts --compile --outfile chat-to-claude-code",
    "test": "bun test",
    "dev": "bun --watch src/server/index.ts"
  },
  "devDependencies": {
    "bun-types": "^1.3.14"
  }
}
